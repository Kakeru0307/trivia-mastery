# 技術トラブル解決記録 (program.md)

開発中に直面した「壁」とその「突破口」のログ

- **Case 1: シェル環境の入力バッファ制限によるファイル破損**
  - **事象**: 長いコード（約160行〜）をヒアドキュメントで流し込もうとすると `unexpected end of file` で失敗する。
  - **解決**: ファイルを分割して追記するか、`generalist` エージェントに一括処理を委託する。
- **Case 2: テンプレートリテラル (${}) の誤展開**
  - **事象**: `run_shell_command` 経由で `${borderClass}` 等を書き込もうとすると、シェルが変数として解釈し、コードが空文字に置き換わる。
  - **解決**: `cat << 'EOF'` (シングルクォート付き) で展開を無効化するか、Python を介して書き出す。
- **Case 3: サンドボックスからの Docker 操作不能**
  - **事象**: `docker: command not found` または権限不足によりコンテナ操作ができない。
  - **解決**: 起動確認はユーザー側（WSL）で行い、Gemini はコードの静的解析とロジック実装に専念する。
